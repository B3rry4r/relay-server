[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "node dist/src/index.js"
restartPolicyType = "ON_FAILURE"
# Pinned to eu-west4 (Netherlands) — closest Railway region to Nigeria.
region = "eu-west4"