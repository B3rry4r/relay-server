[build]
aptPackages = ["git", "curl", "build-essential", "python3", "python3-pip", "python3-venv", "php-cli", "composer", "nix-bin"]

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
